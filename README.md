# Fees Management System (FMS)

This is a scaffolded Django project for a premium Fees Management System. It includes models, admin, basic views, and templates aligned with the provided design.

Next steps:

- Create virtualenv and install requirements
- Run `python manage.py migrate` and `createsuperuser`
- Start the server `python manage.py runserver`
